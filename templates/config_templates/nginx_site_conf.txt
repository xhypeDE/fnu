server {
    listen 80;
    server_name $targetDomain www.$targetDomain;

    location / {
        include proxy_params;
        proxy_pass http://unix:/home/$user/$applicationName/$applicationName.sock;
    }
}